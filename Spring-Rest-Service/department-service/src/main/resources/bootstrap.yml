server:
  port: 8038
spring:
  application:
    name: department-service
  cloud:
    config:
      uri: http://localhost:8038
eureka:
  instance:
    hostname: your-host-name-3
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
