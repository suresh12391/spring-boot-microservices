server:
  port: 8028
spring:
  application:
    name: employee-service
  cloud:
    config:
      uri: http://localhost:8028
eureka:
  instance:
    hostname: your-host-name-2
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
